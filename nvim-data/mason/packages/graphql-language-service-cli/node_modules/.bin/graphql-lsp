#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../graphql-language-service-cli/bin/graphql.js" "$@"
else 
  exec node  "$basedir/../graphql-language-service-cli/bin/graphql.js" "$@"
fi
